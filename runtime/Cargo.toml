[package]
name = "slm-runtime"
version = "0.1.0"
edition = "2021"
description = "SLM-OS Runtime Environment"
authors = ["SLM-OS Team"]

[lib]
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
