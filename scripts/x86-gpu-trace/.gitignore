# Raw mmiotrace output is large (tens of MB) and is regenerated by
# running capture_*.sh on test-pc. Check in only the scripts and the
# small text/log results.
*.trace
nouveau-mmio.stream
sec2_peek
sec2_peek.o
