# Dynamic Policy Model Loading Plan

Archived: April 26, 2026

The implementation work covered by this plan has landed.

The full historical plan now lives at:

- [docs/archive/plans/dynamic-policy-model-loading-plan.md](/home/john/codex/SLM-Operating-System-2-dynamic-policy-model-loading-plan/docs/archive/plans/dynamic-policy-model-loading-plan.md)

Open follow-up work from that plan is now tracked in GitHub issues:

- 🎫 `#448` — generalize runtime policy blob formats
- 🎫 `#449` — extend scheduler runtime payload families
- 🎫 `#450` — finish native file ingress beyond shell-framed upload
- 🎫 `#451` — polish `slm-modelctl` operator ergonomics
- 🎫 `#452` — harden boot autoload corruption and recovery policy
- 🎫 `#453` — broaden operational validation for persistent `/mnt/files` and boot-FAT policy storage
- 🎫 `#454` — define richer boot policy for persisted runtime blobs
- 🎫 `#382` — signed manifest / signed blob integrity policy for runtime HTTP fetches
- 🎫 `#383` — HTTPS transport for runtime model fetches
